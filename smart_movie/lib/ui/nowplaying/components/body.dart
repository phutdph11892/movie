import 'package:flutter/material.dart';

class BodyNowPlaying extends StatelessWidget {
  const BodyNowPlaying({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Now Playing"),
    );
  }
}
