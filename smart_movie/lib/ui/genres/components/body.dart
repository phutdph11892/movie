import 'package:flutter/material.dart';

class BodyGenres extends StatelessWidget {
  const BodyGenres({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
