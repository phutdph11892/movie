import 'package:flutter/material.dart';

class BodyUpComing extends StatelessWidget {
  const BodyUpComing({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Up comming"),
    );
  }
}
