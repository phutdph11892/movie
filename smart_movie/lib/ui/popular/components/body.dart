import 'package:flutter/material.dart';

class BodyPopular extends StatelessWidget {
  const BodyPopular({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Popular"),
    );
  }
}
